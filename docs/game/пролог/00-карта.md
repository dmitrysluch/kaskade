# Пролог: карта контента

Смежные: [[10-пролог]] (замысел), [[07-оболочка-тз]] (формат), [[08-марго]], [[05-тексты-и-документы]]

Здесь лежит **сам контент пролога** в формате оболочки: заметки, которые оболочка
читает как есть. [[10-пролог]] — это замысел и правила, а тут текст, который увидит
игрок.

Тон — по [[05-тексты-и-документы]], «Три регистра»: канцелярит только в бумагах
и в интерфейсе, описания по факту и коротко живым языком, реплики голосом
персонажа. Меты в тексте, который видит игрок, нет нигде.

**Состояние текста:** сцены 2–7 написаны до правила о трёх регистрах и ждут
переборки. Объясняющие строки вычищены, музыка отдана Марго, звонок обеднён,
дубли между сценой 6 и комнатой сняты.

**Что переписывается по итогам разбора** ([[10-пролог]]):

| Что | Где | Статус |
|---|---|---|
| Марго ищет в учебнике ошибку и находит; «тестирую систему на проникновение» | `00-talk`, `00-book` | сделано |
| Реплики Марго пишет игрок; диалог как хаб с необязательными темами | `00-talk` | сделано; сцены 2–7 ждут |
| Экспозиция про оболочку уходит из диалога в `осмотреть учебник` | `00-talk`, `00-book` | сделано |
| Подстановка — отдельная ветка, слово выбирает игрок (жопа/дерьмо/Тоби) | `00-talk` | сделано |
| Тоби — биотехнолог, задел на главу 1 | `items/00-toby`, `00-talk` | сделано |
| Комната — один узел на состояние; глагола `осмотреться` больше нет | все комнаты пролога | сделано |
| Из хаба Тоби можно `вернуться к книге`, не выгоняя его; повторный разговор возвращает в хаб без повторения вступления | `00-room`, `00-talk` | сделано |
| Финальная ремарка про карандашную вставку удаляется: она приписывает необязательную подстановку любому прохождению и повторяет уже сыгранное | `00-talk` | сделано |
| Учебник остаётся одним предметом; обложка и оглавление — секции-состояния с `page:` | `00-book`; [[07-оболочка-тз#Страницы предмета — состояния]] | сделано |
| Фактура: `H 1012`, восьмое издание, `F. Ahlers` на обложке | `00-book`, `00-schedule` | сделано |
| Находка Марго: устаревший факт про седьмой уровень, а не опечатка | `00-talk`, `00-book`, `01-lecture` | сделано |
| Финал замкнут на находку: Алерс формулирует масштаб выброса и спрашивает уровень, Марго отвечает «Семь, как у Чернобыля» | `01-lecture` | сделано |
| Лекция переписана в веер без возврата: вода / конденсатор / слово `ЦИРКОНИЙ`; внутри каждой стратегии один настоящий выбор, все сходятся на катастрофе | `01-lecture`, `10-пролог` | сделано |
| Игрок выбирает действие Марго, а не тему лекции; Алерс отвечает «Сделали» и предъявляет следующее ограничение | `01-lecture`, `10-пролог` | сделано |
| Безусловные переходы из лекции убраны: после провала охлаждения Марго выбирает равноценный диагноз через оголение топлива или рост давления | `01-lecture`, `10-пролог` | сделано |
| Сцена собрана из картинки, а не из скелета: сперва описание аварии, понятное без физики, потом узлы под него. Одна реплика на узел | `01-lecture` | сделано |
| Турбонасос на первом блоке — фактическая ошибка. Заменён изоляционным конденсатором; `RCIC` был на блоках 2 и 3 | `01-lecture`, `reference.yaml`, `10-пролог`, `08-марго` | сделано |
| Добавлен такт про выброс: шкала меряет то, что ушло за забор, — без него цепь обрывалась на взрыве, а находка закрывалась вопросом с известным ответом | `01-lecture` | сделано |
| Ядерная фактура лекции сверена заново без слома лестницы; вход перестроен так, что устаревшую строчку произносит Марго, а не Алерс | `01-lecture`, `reference.yaml` | сделано |
| Реплика Марго не выдаётся безметочным маршрутом: четыре узла отдавали её сами, игрок её не подавал | `01-lecture` | сделано (узлы с тех пор сняты) |
| Лишние факты переехали в `reference.yaml` (`1`): РБМК, быстрые, бор, ОЯТ, проценты | `reference.yaml` | сделано |
| Реплика про коробку снята в черновики: после вывода про записи она создавала второй финал | `01-lecture`, `черновики/01-lecture-вырезанное` | сделано |
| TODO: нарисовать портрет Тоби; `tag: splash:toby` уже стоит, но ассета и палитры пока нет | `00-talk`, будущий `characters/toby/` | **не сделано** |
| TODO: нарисовать портрет Алерса, завести ассет и палитру, затем поставить `tag: splash:alers` на его первое появление | `01-lecture`, будущий `characters/alers/` | **не сделано** |
| Одноответные такты лекции заменены вложенными решениями; объяснение конденсатора распределено между короткими репликами и рисунком на доске | `01-lecture` | сделано |
| Речь Алерса вычищена от разговорного: «вводят», «при тысяче двухстах градусах» | `01-lecture` | сделано |
| «Четвёртый барьер» → «последний»: четыре барьера живут в необязательном узле | `01-lecture` | сделано |
| «Назвать число» → полные ответы, включая «на сколько хватит денег» | `01-lecture` | сделано |
| **Команда короче реплики:** метка — команда строчными, реплика Марго — первой строкой целевого узла | `00-talk`, `01-lecture` | сделано |
| Выход, закрывающий непройденные действия, помечен `advance`: `▶`, отдельная группа и цвет | `07-оболочка-тз`, `00-talk`, `00-room` | сделано в контенте; ждёт оболочки |
| Опции разделены на `environment` / `story` / `system`; `advance` остаётся отдельным признаком сюжетной опции | `07-оболочка-тз` | зафиксировано; ждёт оболочки |
| Разметка: `> —` собеседник, `—` Марго, обычный абзац — ремарка | `00-talk`, `01-lecture` | сделано |
| Метка говорящего `> имя —` там, где собеседников больше одного | `07-оболочка-тз`, `02-club`, `02-neukoelln` | сделано |
| Соглашение «сцена — разговор, комната — пространство» | все `00-*`, `01-*` | сделано |
| Ветка «а если его выпустить» вместо дубля: клапан тоже упирается в электричество | `01-lecture` | сделано |
| Сцены 3–7 (`03`…`07`) под новые правила **не переписаны** | `03-birthday`…`07-titles` | **не сделано** |
| Карнавал переписан в допрос: наутро в отделении, две половины и петля, игрок входит не зная, что они сделали ([[10-пролог]], сцена 3) | `02-neukoelln` | сделано |
| Сцена 8: ведомство 12.10, бланк принят за сорок секунд | новый файл `07-abh-2`, затем титр 2029 | **не сделано** |
| Группа не называется; Тоби думает, что немецкое и старое | `00-talk`, `00-speaker` | сделано |
| Bayer нигде не назван: производитель — вымышленный RHEINWERKE, партии `RWK-…`. **Леверкузен при этом можно**: реальный город при вымышленной фирме — норма, реальный фармгигант как причина эпидемии — нет ([[00-идея]]) | весь проект | сделано |
| Вход сцены 1: находка сыграна в три такта, справка про издания ушла в учебник | `00-talk`, `00-book` | сделано |
| Все белые слова выдаются после явного действия и имеют авторское применение | весь пролог | сделано для актуального набора; `MARK I` удалён, `ПАРОЦИРКОНИЕВАЯ РЕАКЦИЯ` открывает отдельную стратегию на лекции |
| `АЛЕРС, Ф.`: связать автора учебника с лектором; второй путь — через его бумаги | `00-book`, `01-papers`, `01-lecture` | сделано |
| `ФУКУСИМА`: Марго сама называет аварию в конце причинной цепи | `01-lecture` | сделано |
| `ФУКУСИМА`: в 2038-м слово открывает ветку о Библисе | хаб 2038 | **не сделано** |
| `TU BERLIN` убирается из «Дела»; его место занимает `TU-2026/117`, получаемый связью двух источников | `04-abh`, `06-signature`, `words/` | **не сделано** |
| `BLUE CARD` выдаёт `осмотреть карту`, слово открывает ветку в сцене 7 | `05-card`, `06-signature` | **не сделано** |
| `ОБОСНОВАНИЕ` выдаёт `взять бланк`, слово и предмет открывают подачу в сцене 8 | `06-form`, `07-abh-2` | **не сделано** |
| Ветка про Москву обрезана до «единственное, что скачалось»; признания нет | `00-talk` | сделано |
| Тема «реактор против биореактора» убрана; Беломор висит на `prolog.moscow` | `00-talk` | сделано |
| Леверкузен разбит вопросами Марго — три реплики подряд убраны | `00-talk` | сделано |
| Хламидии → гонококк, доксициклин → укол | `00-talk` | сделано |
| Марго один раз проигрывает обмен (`#любишь`) | `00-talk` | сделано |
| Мёртвые узлы `#разбирать`, `#не-напоминай` удалены | `00-talk` | сделано |
| Ремарки добавлены, выходы из тем разной формы | `00-talk` | сделано |
| Звонок не даёт ничего | `items/06-phone` | сделано |
| Стрелка в конспекте не дописана | `items/01-notes` | сделано |
| Музыка ставится Марго | `items/00-speaker` | сделано |
| Сцена 6 не дублирует комнату | `05-dorm` | сделано |
| Вычищены объясняющие строки | `01-lecture`, `02-neukoelln` | сделано |
| Индикатор допроса: «он печатает» только после Тоби, после точных ответов Марго — тишина | `02-neukoelln` | сделано |
| `тоби, молчи` работает трижды и трижды не помогает; про учёбу говорит только Тоби | `02-neukoelln` | **отменено**: у Тоби осталось две ошибки, обе несущие, останавливать нечего |
| Маннитол не объясняется до экспертизы: правда записана по буквам и не работает | `02-neukoelln` | сделано |
| Петля: Тоби спрашивает **у Марго**, надо ли говорить, — их слышат. «Полицейский поднимает голову. Первый раз за утро» | `02-neukoelln` | сделано |
| Ставка названа процедурой: расфасовка, слово «сбыт», последствия для ВНЖ одной будничной репликой | `02-neukoelln` | сделано |
| Вид на жительство изъят в начале и лежит под лампой до финала | `02-neukoelln` | сделано |
| Правда сказана сразу и не работает: слова не знает никто в комнате. Тень падает на бумагу, не на Марго | `02-neukoelln` | сделано |
| Карточка `word-obosnovanie` фиксирует факт, а не вывод | `words/word-obosnovanie` | сделано |

## Принятые правки `00-*` после повторной вычитки

### Разговор можно прервать, не выгоняя Тоби

Одного `advance` на `поговорить с Тоби` недостаточно. На первом игровом экране
это превратило бы обычный вход в диалог в ловушку порядка: игрок ещё учится
командам, но уже обязан догадаться сначала `осмотреть Тоби`, иначе навсегда
теряет наклейку и леверкузенскую ветку.

В хаб `00-talk#хаб` добавляется обычная сюжетная опция `вернуться к книге`:

- она возвращает в первое состояние `00-room`;
- Тоби остаётся в комнате, `осмотреть Тоби` и остальные предметы доступны;
- пройденные `once`-темы разговора не восстанавливаются;
- повторное `поговорить с Тоби` ведёт сразу в `00-talk#хаб`: находка про два
  седьмых уровня, портрет Тоби и «Терешкова!» второй раз не проигрываются;
- `поговорить с Тоби` и `вернуться к книге` не получают `advance`, потому что
  ничего не закрывают;
- `▶ иди уже` остаётся единственной точкой невозврата: после неё Тоби уходит и
  непройденные темы исчезают.

Для проводки нужен флаг первого входа, например `prolog.talk-started`. Первый
переход из комнаты ведёт во вступление `00-talk`, повторный при установленном
флаге — прямо в `#хаб`. Флаг ставится до первого появления хаба и отвечает
только за маршрут; состояние `once` по-прежнему хранит сам движок.

### Финал не комментирует необязательную подстановку

После строки:

> Из коридора слышно, как он спрашивает на кухне, кому что взять.

сцена сразу возвращается в `00-room#один`. Строки «Ты возвращаешь книгу к себе.
Карандашная вставка на третьей строке — там же, где была» удаляются целиком, без
условной замены.

Причины две. Вставка появляется только в необязательной теме `смотри`, поэтому
нынешний финал врёт при раннем выходе. Кроме того, обязательная находка Марго —
обведённый устаревший абзац, а вставка относится к другой шутке; финал смешивает
два карандашных следа и повторяет уже показанное.

### Ремарка перестала подписывать реплики

В сценах с одним собеседником `> —` — это он, и вопроса «кто говорит» нет.
В `02-club` говорящих пятеро, в `02-neukoelln` двое, цвет `speech` у них общий,
и различать их приходилось прозой: «Спрашивает парень справа, не бармен».

Это тратило ремарку на служебную работу. **Ремарка описывает действие; кто
говорит — обязана сообщать разметка.** В [[07-оболочка-тз]] заведена
необязательная метка: всё, что стоит между `> ` и первым ` — `, читается как имя
говорящего, пусто — собеседник сцены по умолчанию.

```markdown
> полицейский — Пакетики ваши?
> тоби — Это был эксперимент.
```

Метка строчными, как команда, печатается цветом `remark`; отдельного цвета
на каждого говорящего не заводится — их может быть сколько угодно, а различать
должно имя, а не оттенок, который придётся запоминать.

`00-talk` и `01-lecture` не тронуты: там собеседник один, и метка была бы шумом.

Одна ремарка после этого осталась и должна остаться: «Тоби отвечает раньше
тебя». Она сообщает не кто говорит, а что он перебил, — то есть делает работу,
которой у метки нет.

### Сцена 3 пересобрана: клуб, участок, экспертиза

Прежняя версия держалась на удержании тайны: карнавала и клуба на экране нет,
Марго ни разу не врёт и не уходит от ответа, а игрок узнаёт правду вместе
с полицейским. Новая дока ([[10-пролог]], «3. Карнавал, клуб и отделение
полиции») отменяет и то, и другое. Реализовано так.

**Завязка стала тремя монтажными кадрами** в отдельном файле `02-club`. Игрок
видит раздачу и драку своими глазами — управления ему при этом не дают
([[07-оболочка-тз]], «Монтажный кадр»): ни опций, ни фиктивного `дальше`,
только `Enter` между кадрами.

| Кадр | Что в нём |
|---|---|
| `(корень)` | карнавал остался снаружи, случайный клуб, «на одну» |
| `#раздача` | Тоби отдаёт первый пакетик и честно называет вещество; Марго просит себе и **второй передаёт сама** |
| `#драка` | клубный слух про «биохимика», отсутствие эффекта, стакан, стекло, «вот они, оба раздавали», сирена |

Слух переехал из кадра раздачи в кадр драки, и там он не украшение,
а её причина: получатели ждали того, что им пообещал не Тоби, а сосед
по танцполу. Заодно кадры выровнялись по весу — 30, 76 и 62 слова вместо
одного на сто с лишним.

**Участие Марго перестало быть техническим.** Она передаёт пакетик сама,
поэтому в участке её считают участницей раздачи по показаниям свидетеля,
а не по доносу Тоби. Страх за ВНЖ перестал браться из воздуха.

**Первая развилка стоит не на составе вещества, а на «Пакетики ваши?»** —
иначе жёсткая ветка объяснила бы игроку маннитол на десять узлов раньше срока.
`margo.procedural` уходит в статус разговора и отказ пояснять до экспертизы,
`margo.confrontational` — в «Мои. На них всё написано» и «Я ответила точно».
Обе ломает одна будничная реплика про последствия для вида на жительство,
и обе сводятся к одному вопросу: что останется в записи. Дальше расходится
только способ требовать точность — `прочитайте дословно` против
`можно увидеть, что вы записали`.

**Две ошибки Тоби вместо пяти.** Он больше не поправляет подряд число
пакетиков, деньги и биографию Марго. Осталось ровно две реплики: «Это был
эксперимент» (с `prolog.pentest` — «Это был тест на проникновение») и «Моя.
Марго только согласилась помочь». Первая устанавливает намеренность, вторая —
благородная. Про учёбу он не говорит вовсе: её карта и так лежит под лампой,
это не новое сведение.

**У Марго появился ход, которого раньше не было:** отказаться от его
прикрытия. `это неправда` → «Один пакетик я передала сама». Старое правило
«она никогда не врёт и не уходит от ответа» снято докой, и это первое место,
где снятие видно.

**Развязка перестала быть снятием обвинения вообще.** Экспертиза говорит про
вещество, а не про право: маннитол, пищевая добавка, контролируемым веществом
не является. Полицейский не произносит «нарушения не было» и не говорит
«Порядок». Запись о доставлении он не трогает, когда правит показания, —
это другая бумага, и слово «протокол» больше не обозначает всё сразу.

**Финал не даёт автоматического согласия.** На «это была лучшая ночь за весь
год» у игрока три ответа, включая «У тебя разбита губа. У меня осталась
запись».

Счёт: 3 узла клуба, 1 запись, 25 узлов участка и улицы. Дока просила
двенадцать-шестнадцать на обе части участка; ядро укладывается в семнадцать,
остальное — две ветки стратегии, два варианта реплики Тоби и три финальных
ответа, то есть цена ветвления, а не длина спины.

> **Монтажный кадр не подчиняется правилу «не больше двух реплик подряд».**
> Правило существует ради того, чтобы ход игрока был близко; в кадре хода нет
> по устройству, и диалог целиком — законная единица.

### Два флага, которые были отрицанием друг друга

`margo.procedural` и `margo.confrontational` ставятся на двух опциях одной
развилки, и других источников у них нет. Значит, на входе в узел, который их
читает, всегда стоит ровно один — никогда оба и никогда ни одного.
`confrontational` тождественно `!procedural` и не несёт ни бита.

Избыточность тут не главное. Главное — **обе опции узла `#цепочка` были
условными**, и инвариант «ровно один флаг установлен» не проверял никто: движок
не знает, что условия дополняют друг друга, а графовая проверка считает
переходы, не заглядывая в их `if`. Стоило бы появиться третьему входу
в развилку или обходу мимо неё — и узел остался бы без единой опции, то есть
тупиком посреди сцены. Дока прямо предупреждает, что вторичные выборы обеих
частей участка ещё не выбраны, так что это не гипотетический случай.

Разведено так: **ставятся оба, читается один.**

- ветвление идёт по `margo.procedural` и `!margo.procedural` — ровно как
  `prolog.pentest` в лекции, и узел не может потерять все опции;
- `margo.confrontational` остаётся положительной записью о характере, наравне
  с `margo.demonstrative` и `margo.sent-people`. «Давила точностью» — это факт,
  а не отсутствие процедурности, и в 2038-м спрашивать его будут именно так.

> **Бинарную развилку читают одним флагом и его отрицанием. Второй флаг
> заводят, только если он положительный факт, — и тогда его не читают.**

### Имя нельзя было назвать посреди разговора

`меня зовут марго` стояло обычной темой хаба, наравне с «как тебе трек» и «что
в ноуте», и ломалось это двумя способами сразу.

**Тема необязательная — значит, фамилии могло не прозвучать вовсе.** Тоби зовёт
её Терешковой и в первой реплике, и на прощание; игрок, невнимательно
проглядевший личное дело на сплэше, честно выходил из сцены, считая Терешкову
фамилией.

**Тема в хабе — значит, её можно выбрать пятой.** «Меня зовут Марго. Гринберг» —
ответ на оклик, а не реплика. Сказанная через десять минут разговора про техно
и гонококк, она приходит ниоткуда.

Обе половины чинятся одним переносом: развилка уходит из хаба и встаёт **сразу
после оклика**, до входа в хаб. Пропустить её нельзя, выбрать не вовремя —
тоже. Выбор при этом остаётся: `меня зовут марго` открывает прежнюю ветку
про Grünberg и идиш, `промолчать` уводит в новый `#прозвище`, где работу делает сам Тоби:

> — Ладно. Марго.
>
> Он говорит это в потолок.

Он окликнул прозвищем, не получил ответа и перешёл на имя. Этого хватает,
чтобы игрок увидел: Терешкова была шуткой. Марго при этом не сказала ни слова.

Первая версия узла была ремаркой — «Терешкова — это с сентября. Ты объясняла
один раз» — и не работала дважды. «Ты объясняла» не говорит, кто кому и что
объяснял, а читается так, будто прозвище выбрала она сама. И главное:
**на ход `промолчать` нельзя отвечать объяснением.** Если Марго смолчала,
то и рассказчику объясняться не за что — иначе ход теряет ровно то,
ради чего его выбрали.

> **Молчание отыгрывает собеседник, а не рассказчик.**

> **Реплика-ответ не бывает темой хаба.** Если строчка отвечает на конкретный
> оклик, её место — сразу за окликом; хаб для тем, которые игрок заводит сам.

`prolog.soft-grunberg` намеренно **не** ставится в `#прозвище`. Флаг кормит
«Ты чего сегодня тихая?», а это про сегодня: не поправить старое прозвище —
привычка, а не замыкание. Промолчать на «ты почти наша» — замыкание.

### Беломорканал стоит только там, где он к месту

Шутка `#беломор` — «Беломорканал не завезли, ты извини» — обвешана двумя
условиями, и оба нужны.

**Флаг.** Узел закрыт `if: prolog.moscow`. Без него Тоби не знает, что она
из Москвы, и шутка про советскую марку висит в воздухе. Флаг ставится
в необязательной ветке `#весь-полёт`, так что у большинства прохождений
шутки не будет вовсе — и это правильная цена.

**Место.** Оба ребра шли из узлов после вопроса «Тебе Marlboro красный или
Parliament?», то есть формально всё было верно. Но одно из них шло из
`#после-ничего` — ветки, где Марго отказалась. Там Тоби уже пожал плечами
и уходит, а «ты извини» подразумевает, что она чего-то хотела. Ребро снято:
шутка живёт только после `#красный`, где она назвала марку и он теперь
её поддевает.

> **Условие «после чего» бывает недостаточным. Проверять надо не порядок
> узлов, а то, что персонаж в этот момент только что сделал.**

### Подстановка была подписана чужим голосом

`#жопа`, `#дерьмо` и `#тоби-слово` печатали прочитанный вслух абзац цитатой:

```
Ты правишь три слова карандашом и читаешь вслух.

> Оболочка рассчитана на проектную жопу — жопу, условия которой…
```

Голый `>` в таблице «Кто говорит» не определён, и рендерер красит его как
реплику собеседника. Получалось, что главную шутку сцены произносит Тоби.
Абзац читает вслух Марго, значит и разметка её: одна строка, начатая `—`,
переносит её оболочка сама.

Второе следствие того же места — предпросмотр. `preview` брал **первую строку
узла**, а первой строкой стоит ремарка «Ты правишь три слова карандашом…».
Игрок выбирал `жопа`, `дерьмо` или `тоби`, не видя, во что это развернётся, —
то есть ровно в тех трёх опциях, где вся соль в формулировке, правило
«команда — ядро реплики» не работало.

Правило в [[07-оболочка-тз]] уточнено: оболочка читает узел сверху, пропускает
ремарки и показывает **первую реплику Марго**; если раньше неё встретилась
реплика собеседника, область остаётся пустой. Длинную строку разрешено обрезать
по границе слова с многоточием — дословность головы фразы и есть то, ради чего
предпросмотр существует.

> **Ремарка — не реплика. Область реплики показывает только то, что Марго
> действительно произнесёт.**

### «То же самое» ломалось на одной ветке из трёх

Строка про кухню держалась на том, что игрок помнит вопрос про сигареты. На
двух путях из трёх это работало. На третьем перед ней успевал прозвучать
`#тихая` — «Ты чего сегодня тихая?», — и «то же самое» цеплялось за ближайший
вопрос: выходило, что Тоби ходит по общаге и спрашивает у всех, чего они тихие.

Условного узла заводить не пришлось: чинится сам референт. «Спрашивает на кухне,
кому что взять» называет заказ, не называя сигарет, и работает после любой
последней реплики. Шутка на месте — он собирает заказы по всему этажу, — а
двусмысленности больше нет.

> **Указательное слово чинят не ветвлением, а именем.** Если «то же самое»
> ломается на одном из путей, дешевле назвать вещь, чем разводить узел
> по флагу.

### Точное число про человека приходит с бумаги

В `00-talk` возраст Алерса дважды звучал цифрой: Тоби говорил «у деда, которому
семьдесят два», Марго повторяла «Ему семьдесят два, а не двенадцать». Ни один
из них этого знать не может. Тоби видит на обложке фамилию и год издания,
Марго готовилась по книге, а не по личному делу.

Осталось: Тоби — «И ты весь день ищешь ошибку у деда», Марго — «Ему
не двенадцать». Реплики стали короче, а «дед» и без цифры делает ровно ту
работу, ради которой цифра стояла.

Точное число никуда не делось — оно ушло туда, где ему место. В `01-hall`
он «человек лет семидесяти на вид»: так его видит Марго. В `03-birthday`
объявление на двери кафедры пишет «профессору Алерсу семьдесят три» — и это
первый раз за пролог, когда игрок получает число. Оно сходится: июль
2025-го минус семьдесят три даёт те же семьдесят два в октябре 2024-го.

> **Персонаж говорит то, что мог узнать. Точное число, дату и номер
> предъявляет документ.**

### Один учебник, две страницы-состояния

Не создавать отдельные предметы «обложка» и «оглавление»: физически это одна
книга, а новые предметы раздуют первый список команд. Используется
общий формат [[07-оболочка-тз#Страницы предмета — состояния]]: каждая страница —
секция `00-book` с заголовком-id и атрибутом `page:`.

| Секция | `page` | Что остаётся в ответе | Эффект |
|---|---|---|---|
| `## обложка` | `1` | библиотечный экземпляр, название, `K. Schmidt, F. Ahlers` | начальная страница |
| `## оглавление` | `2` | первая глава — шкала событий, вторая — определения | без выдачи слова |

Перечень четырёх барьеров из первой карточки убрать: для сцены `00` он ничего не
открывает, на лекции нельзя требовать необязательного знания, а с именем Алерса
он конкурирует за внимание. Вклейка с `Mark I` позже удалена целиком: слово не
получило отдельной игры, а кольцо путало схему аварии.

Первое `осмотреть учебник` показывает обложку. После него движок предлагает
`листать учебник вперёд`; на оглавлении доступно только направление назад.
Повторный осмотр показывает текущую страницу. Заголовки `обложка`, `оглавление`
игрок не вводит: это стабильные id состояния
для движка и сейва.

Проводка контента:

- заменить `## осмотреть` двумя секциями с `page: 1`, `2`;
- не добавлять специальные генераторы в `00-room` и глаголы в `itemVerbs`:
  перелистывание строит движок для любого предмета с `page:`;
- учебная подсказка `попробуйте: осмотреть учебник` не меняется: она открывает
  первую страницу, но не выдаёт награду заранее.

## Как чинилась ядерная фактура лекции

Третий лог решения после «Как искалась находка Марго» и «Как карнавал стал
отделением полиции» ([[05-тексты-и-документы]]), по той же причине: ошибка
типовая и повторится в других сценах.

`01-lecture` драматургически работала с самого начала. У Марго есть ясное
желание — поймать Алерса на книге; он не оправдывается, а превращает её находку
в задачу; аварию собирает сам игрок из собственных ответов; вопрос про стену
возвращает сцену к теме внешней угрозы. Конструкцию не трогали.

Ломалось другое: **авторитет Алерса создаёт ритм сцены, а технические реплики
его подрывали.** Чем увереннее он говорит, тем заметнее ошибки человеку, который
хоть немного знает Фукусиму. Шесть штук и одна логическая дыра.

### Шесть фактов

| Было | Стало |
|---|---|
| «Седьмой уровень: выброс большей части активной зоны за пределы площадки» — не определение седьмого уровня, а вдобавок оценка `7` относится к аварии целиком, и сцена незаметно клеила её к первому блоку | настоящее определение: крупный выброс с широкими последствиями для здоровья и среды. Оценку получает авария, а не блок. Отсюда же вырос такт про выброс: раз шкала меряет то, что ушло за забор, цепь обязана до этого дойти |
| «Водо-водяной» реактор в такте 2 и `Mark I` в такте 3: гибрид из PWR и BWR, собранный без предупреждения | разбирается **один блок, кипящий реактор**. Тип называется один раз, обычными словами, там, где начинает работать. Код `Mark I` остался эксклюзивом необязательной ветки |
| «А воду откуда брать?» → «Море. Река. Пруд-охладитель. Градирня» — смешаны теплоноситель, аварийный запас и конечный поглотитель тепла. Реплика смешная и потому особенно опасна: игрок уносит красивую неправду | ветка осталась про воду, перечень стал настоящим аварийным запасом: бак с конденсатом, кольцо под реактором, дальше море. Куда станция девает тепло в штатном режиме, к этой аварии отношения не имеет, и ветки про это больше нет |
| «Нет дизелей — батареи» → «Сколько держат батареи?» → батареи восемь часов гонят воду. Вымышленный блок из деталей нескольких настоящих | на первом блоке был **изоляционный конденсатор**: бак с водой выше реактора, восемь часов на одной гравитации. Убили его собственные четыре задвижки — электрические, закрылись сами по отказобезопасной логике. Такт «аварийное охлаждение питается от того, что ты только что заглушил» перестал быть метафорой |
| «Последний барьер при этом вскрывается изнутри» — заставляет представить взрыв первичной оболочки и ломает пространственную логику `Mark I` | оболочку вскрывает давление, а не взрыв; водород уходит в здание над ней, и взрывается здание. Заодно восемь часов оказались бумажными: батареи стояли в тех же подвалах, что дизели |
| «Инженер. Один раз, при проектировании. Дальше её не пересматривают» — сильный афоризм и неверная история реального провала. И `5,7` — не высота стены, а расчётный уровень цунами (площадка стояла на десяти, залило на четырнадцать-пятнадцать) | `5,7` называется расчётной волной. Ложный тезис заменён механизмом: пересчитывают, когда меняются записи, — а в записях нет ничего, чего ещё не было. Реплики `#основание` не тронуты, снят только вопрос про шкалу: он переехал в такт 6 и достался Марго |

### Дыра: книга и «Знаю»

Вход был устроен так: Алерс открывает **свою** книгу, без оговорки читает
устаревшую строку «Единственный случай в истории», Марго поправляет — «Их два»,
он отвечает «Знаю». При этом `01-papers` заранее показывает распечатку в
рукописных правках и ту же книгу с тремя закладками, а в финале он собирает
бумаг больше, чем прочёл. Это человек, который готовил лекцию, а не впервые
увидел старый абзац за кафедрой.

Последовательность не складывалась ни в одну версию. Ошибся — «Знаю» звучит как
дешёвая попытка сохранить лицо. Знал — непонятно, зачем выдал студентам ложный
факт.

**Что пробовали.** Оставить реплики как есть и показать ремаркой, что он уже
вычёркивал эту строку карандашом. Не работает: пока он произносит устаревший
факт вслух, он подготовленный человек, который по-тупому ошибается, и никакая
постановка этого не отменяет. Плюс Марго не видит с четырнадцати метров, куда
идёт его карандаш, а игрок не заметит ремарки.

**Что решило.** Устаревшая фраза — вторая строка абзаца. Он читает первую
(определение, оно верное) и **закрывает книгу, не дочитав**. Строчку из абзаца
достаёт сама Марго — это её ход и её команда. «Знаю» становится законным: он
этого не говорил, он это пропустил.

Побочные выигрыши, которых не искали:

- **Засада проваливается сильнее прежнего.** На «там дальше есть ещё строчка» он
  отвечает «Есть», не открывая книгу, и цитирует по памяти. Страницу, над которой
  она сидела всю субботу, он знает наизусть.
- **Игрок это замечает**, потому что оба такта — его ходы, а не ремарки.
- `01-hall#до` («Ты знаешь, что скажешь. Ты знаешь, на какой странице») окупается
  буквально.
- Финальная ремарка «Он закрывает книгу на той же странице» стала невозможной и
  заменена на «Книга так и лежит закрытой»: три часа субботы, и страница была
  открыта одно предложение.

Правило, которое из этого выводится:

> **Ложный факт не обезвреживается постановкой. Если персонаж не должен
> ошибаться — он не должен произносить.**

### Вторая итерация: фактура сошлась, а сцена встала

Первая правка была технически верной и нечитаемой. Что сломалось и чем чинилось:

| Что | Чем |
|---|---|
| **Порядок тактов.** Взрыв стоял третьим, насосы четвёртым: сцена доходила до вершины и возвращалась к сантехнике. После взрыва вернуться к «а чем воду гонять» нельзя | такты 3 и 4 переставлены. Сначала выясняется, на чём держится вода, потом показывается, что бывает без неё. Лестница пошла строго вниз по причине |
| **Два узла с одинаковым содержанием.** `#точно` (за слово `ПАРОЦИРКОНИЕВАЯ РЕАКЦИЯ`) и `#водород` говорили дословно одно и то же — слово не покупало ничего | развели. Обычный ответ получает реакцию и водород; слово покупает то, чего нет на общем пути: реакция греет сама себя, чем горячее — тем быстрее |
| **Неясная география.** «Оболочка стоит внутри здания. Водород уходит туда» — читателю негде поставить водород и нечего представить | геометрия называется вслух и по-русски, тремя стенками: корпус в оболочке, оболочка в здании. И сразу задача на три хода: «Две он пройдёт, третью снесёт». Дальше — что именно расходится (стыки) и что именно взрывается (верхний этаж здания). Ключевая строка: «Оболочка при этом стоит. Дырявая, но стоит» |
| **Кольцо появлялось из ниоткуда** и уводило от водорода обратно к охлаждению | ветка переехала к течи и теперь объясняет ровно одно: почему оболочка потекла так рано. Заодно кольцо посеяно на такт раньше, в перечне аварийного запаса |
| **Финал был предрешён.** Сцена открывалась седьмым уровнем и закрывалась вопросом «так где он на шкале» | вопроса больше нет. Вход обрывается на «Второй случай был позже. Его и разберём», а закрывает находку сама Марго: узнав, что шкала меряет выброс, говорит «Значит, семь» |
| **Марго ошибалась.** Ветка «Реактор заглушен. Он холодный?» → «Холодный» противоречит [[08-марго]]: материал она знает отлично | оба ответа верные. `нет` — осколки распадаются сами; `семь процентов` — тот же ответ, поданный демонстративно. Он не хвалит, он пишет число на доске |
| **Ремарки через отрицание.** «Он не открывает книгу», «Он не поднимает головы», «Никто не оборачивается», «Станцию он не называет» — четыре подряд | физическое действие: «Книга лежит там, где он её оставил», «Он стирает угол доски», «В третьем ряду листают телефон», «Он ждёт». Отрицаний осталось два, оба намеренные |
| **«Три часа субботы»** — ремарка ни к кому | «Ты потратила на это три часа субботы» |

Правило отсюда:

> **Причинная лестница задаёт и порядок сцены. Такт, который объясняет
> устройство, идёт до такта, который его ломает, — иначе сцена после вершины
> возвращается назад и встаёт.**

### Третья итерация: сначала картинка, потом сцена

Порядок починили, а читать всё равно было тяжело. Диагноз: две первые итерации
собирали сцену **от скелета к фактам** — брали существующие узлы и вставляли в
них верные сведения. Получался набор корректных утверждений, не складывающихся
в картинку: длинные фразы, блоки по три реплики, соседние реплики без связи.

**Что решило.** Сначала сформулировано само описание аварии — такое, что его
понимает человек без физики, — и только потом под него подстроены узлы.
Описание из девятнадцати пунктов, каждый одно предложение, каждый следует из
предыдущего: кипятильник → деление гасится за две секунды, обломки греют сами →
воду надо гонять всегда → воду гонят насосы, насосам нужен ток → сети нет,
дизели и батареи в подвале → одно тока не просило: бак выше реактора, восемь
часов на гравитации → у бака четыре электрические задвижки, закрылись сами →
его выключило то, чего не было → топливо оголяется → цирконий берёт у пара
кислород → водород вверх, три стенки, две пройдёт и третью снесёт → стык у
фланца расходится → верхний этаж сносит → оболочка стоит дырявая → через тот же
стык уходит йод и цезий → это и меряет шкала → волну считали на пять и семь,
площадка на десяти, пришло четырнадцать.

Правила письма, выведенные из этого:

- **Одна реплика на узел.** Два `>` подряд — объявленное исключение (сейчас
  11 узлов из 46, 23%). Три — только `#сборка` с `tag: monolog`.
- **Узел кончается вопросом Алерса**, а не утверждением: ход игрока — ответ,
  а не «продолжить».
- **Соседние реплики в узле связаны.** Если вторая не следует из первой —
  это два узла.

Медиана реплики после переборки — девять слов, максимум шестнадцать.

Заодно исправлена фактическая ошибка второй итерации: **на первом блоке был не
турбонасос, а изоляционный конденсатор.** Турбонасос `RCIC` стоял на блоках 2
и 3. Замена не только чинит факт — она короче (одна вещь вместо турбины плюс
клапанов на батареях), объясняется без физики и точнее попадает в тему:
восемь часов теперь висят на том, на чём висели в реальности.

Три связки, которых до этой итерации не было:

| Связка | Что делает |
|---|---|
| «Что осталось?» → «Ничего.» → «Одно осталось.» | тупик объявляет игрок, и конденсатор появляется как последняя надежда, а не как очередной пункт списка |
| «Его выключило отсутствие тока.» → «Да.» | вывод принадлежит Марго, Алерс не комментирует. Дальше пауза |
| «А оболочка?» → «Стоит. Дырявая, но стоит.» | два коротких хода вместо абзаца. *Снято при следующей чистке: устояла оболочка или нет — сюжету всё равно, а щель под крышкой устанавливается тактом раньше* |

Правило отсюда:

> **Сцену пишут из картинки, а не из скелета. Сначала объясни событие так,
> чтобы понял человек без предмета, и только потом режь объяснение на узлы.
> Обратный порядок даёт верные фразы, которые ни во что не складываются.**

### Четвёртая итерация: какие факты вообще несут аварию

Картинка помогла, но спина осталась на тринадцать шагов, и логика в ней всё
равно терялась. Ошибка была в том, **что резали**: реплики склеивали, а список
фактов не трогали. Правильный вопрос не «как покороче сказать», а «что из этого
вообще нужно».

Тест, по которому пересобрали:

> **Уберём — рвётся ли причинная цепь? Если авария происходит ровно так же,
> факт не несущий, каким бы верным он ни был.**

Несущими оказались десять фактов: реактор заглушен и всё равно греется; тепло
надо снимать всё это время; бак на гравитации, восемь часов; у бака
электрические задвижки; тока нет; воды нет — топливо оголяется; цирконий берёт
у пара кислород и остаётся водород; давление в оболочке выше проектного и стык
у фланца расходится; водород ушёл в здание и снёс его, а оболочка устояла
дырявой; через тот же стык ушли йод и цезий.

Не прошли тест:

| Что | Почему |
|---|---|
| **насосы, сеть, дизели** | три хода ради одного вывода «тока нет», а на первом блоке охлаждение делал бак на гравитации — насосов в этой цепи нет вообще. «Тока нет» стало условием задачи: где именно он пропал, разбирается в `#сборке`, и в этом весь её смысл |
| **уран, турбина, «это кипятильник»** | турбина к аварии отношения не имеет |
| **замедлитель, «деление прекратится»** | тупик, признан ещё в третьей итерации |
| **три стенки** | нужны две: оболочка и здание. Корпус реактора не участвует ни в одном следующем шаге |
| **«какой взрыв — химический»** | не звено цепи, а успокоение для тех, кто думает про бомбу. Уехало в справочник |

Спина стала пятью шагами: **заглушен, но греется → чем снимать тепло, когда нет
тока → бак на гравитации, и его задвижки электрические → воды нет, цирконий даёт
водород → стык у фланца расходится.** Ходов игрока между «Его и разберём» и
«Фукусима» — пять вместо тринадцати, узлов в файле восемнадцать вместо
сорока пяти.

Шаг про стык не режется, потому что несёт двойную нагрузку: **дыра, которая
выпускает водород, — та же, через которую потом уходит выброс.** Одна дыра,
два следствия, и обе половины такта про шкалу держатся на ней.

Правило отсюда:

> **Режут не реплики, а факты. Склеенные реплики дают ту же лестницу, только
> плотнее; лестница становится короче, только когда со ступеньки снимают
> то, из чего ничего не следует.**

### Пятая итерация: характер Марго как источник веток

Скелет работал, но у Марго на нём были только простые ответы — а она лучшая на
курсе и любит это показывать. Это не недостаток текста, а неиспользованный
ресурс: **её характер и есть источник веток.**

Три приёма, которые из этого выросли:

**1. Ответ бывает не «правильный/неправильный», а правильный не для этого
блока.** На «чем снимать тепло» три ответа: `ничем` (сдалась), `турбонасосом`
(есть такой, но на втором и третьем блоках) и `изоляционным конденсатором`
(назвала сама — и тогда устройство бака объясняет она, а Алерс только просит:
«Им. Расскажите»). То же в такте про давление: `пар` — не ошибка, а половина
ответа, потому что пар уходит в кольцо и снова становится водой.

> **Легально путать игрока можно, если следующая реплика выводит на нужное.**
> Ошибка в характере лучше, чем правильный ответ без характера.

**2. Инверсия ролей.** С момента, когда конденсатор выключился, Алерс перестаёт
объяснять устройство и начинает называть показания, а диагноз ставит Марго:
«Давление в оболочке восемь атмосфер. Проектное — четыре. Откуда?» Смена режима
совпадает с моментом, когда всё пошло не так, и вместе с ней меняется время
глагола: было «уберите, чем снимать» — задача, стало «привезли, сняли, открыли»
— отчёт.

**3. Хаб «Что вы будете делать?»** — три реальные импровизации, каждая
возвращает в хаб, выход помечен `advance`. Игрок изобретает, изобретение
оказывается ровно тем, что делали на самом деле, и ровно так же не помогает:

| Что предлагает игрок | Что было |
|---|---|
| доставить насос | пожарные машины → баки кончились к утру → море, и решали четыре часа → подали трубами пожаротушения, а обратных клапанов в них нет, и половина ушла в конденсаторы |
| найти питание | сняли аккумуляторы с машин на стоянке → хватило на приборы, стало видно уровень → двенадцать вольт, а приводу задвижки нужно сто двадцать пять |

Пара читается как целое: импровизировали воду и питание — и ничто не спасло.
Это и есть тема, сыгранная механикой, а не сказанная вслух. Третья
импровизация, сброс давления, из хаба уехала: он не охлаждает, и её место —
после взрыва, где клапан существует ровно чтобы этого не было.

Попутно: **`фланец` убран** — вместо него «съёмная крышка на болтах, между ней
и корпусом прокладка», и дальше «прокладку выдавило». Через ту же щель потом
уходит выброс.

Спина выросла с пяти шагов до семи (оба новых — от инверсии), узлов стало
полсотни. Ходов игрока — двадцать три.

### Шестая итерация: маршрут склеивает блоки

Проверка врала. Я считал реплики подряд **внутри узла**, а игрок читает
**цепочку безусловных маршрутов целиком**: узел с одной репликой плюс маршрут
в узел с двумя — это три реплики в одном блоке. Скрипт этого не видел, глаз
увидел сразу.

> **Безусловный маршрут склеивает блоки. Считать надо после склейки.**

Таких мест оказалось шесть. И главное — **чинить их переносом строки нельзя**:
содержание остаётся тем же, читается тем же куском. Чинится только двумя
способами:

| Способ | Где применён |
|---|---|
| **выделить ход игрока** между узлами | `#цирконий` → `и он давит на оболочку` → `#крышка`; `#ничем` → `что` → `#бак`; `#бак` → `и что с ним стало` → `#задвижки`; вход в `#сборка` через `промолчать` |
| **упростить: слить узлы и убрать один** | `#стена` слит с `#семь` — три числа на доске (`5,7`, `10`, `14`) вместо отдельного узла |

Цена — спина выросла. Это осознанный размен: **короткие блоки читаются быстрее
длинной спины.**

Дальше правило применено ко всей сцене, а не только к склеенным местам:

> **Одна короткая реплика Алерса на узел, и она же вопрос.** «Кончились
> к утру. Откуда ещё?» — реакция и вопрос в шести словах. Игрок отвечает,
> и это следующий узел.

Ни один блок не длиннее двух коротких реплик, и сцена читается быстрее
прежней. Точные числа — в «Проверено» ниже.

**Седьмое место нашлось позже — в самом финале.** `#записи` уходил безусловным
маршрутом в `#молчание`, и склейка давала три реплики Алерса подряд. Между
выводом про записи и сбором бумаг добавлен ход `собрать вещи`: он объясняет,
почему Марго остаётся в аудитории, пока остальные выходят.

Финальная реплика про коробку всё равно создавала второй авторский вывод после
уже состоявшегося финала. Она снята целиком и перенесена в
`черновики/01-lecture-вырезанное.md`; ход оставлен ради ритма и физики сцены.

### Веер выявил новый класс ошибки: состояние доски зависит от пути

Лекция переписана из лестницы в веер, и разом сломалось то, что в лестнице
не могло сломаться. **Доска — это накопитель, а ремарки писались так, будто
порядок один.**

`#прокладка` тянула стрелку «из оболочки… под крышу здания», `#взрыв`
перечёркивал «верхний этаж» — а оболочку и здание рисовал только `#давление`,
мимо которого проходят два схождения из трёх. Хуже: `#выпустить-водород` вёл
линию «от оболочки» ещё до того, как `#давление` её нарисует, — то есть на этой
ветке порядок был вывернут, а не просто неполон.

Чинится это не в ремарках, а в корне: **всю геометрию Алерс рисует один раз,
до первого решения.** Три контура один в другом — реактор, оболочка, здание, —
и внутри реактора топливо с уровнем воды над ним. Так и было бы на настоящей
лекции: сначала предмет, потом происшествие. Итеративность рисунка от этого
не теряется, потому что она не в геометрии: дальше дорисовываются насос, бак,
задвижки, клапан, падает уровень воды и взрывается верхний этаж.

> **Геометрию рисуют один раз до первого решения, события — по одному за ход.
> Референт обязан существовать на всех путях, а не на том, по которому писали.**

Уровень воды был третьим таким референтом и самым незаметным: `Он опускает
линию воды ниже верхнего края топлива` стояло на водной ветке, хотя никакого
уровня на доске не было — рисовали только трубу от насоса. Теперь уровень
в базе, ремарка называет его уровнем, а не линией, и опускается он на **всех**
ветках, включая конденсаторную, где раньше топливо оголялось вообще без следа
на доске.

Тот же класс — **цирконий, и он оказался хуже прочих: слово не имело хозяина
вообще ни на одном пути.** Марго называла его первой — в `#ещё-газ` («Цирконий
отбирает у пара кислород») и в вопросе «Почему цирконий вообще?». До этой
секунды металл в сцене не звучал, то есть игрок не мог ни знать его, ни выбрать.

Чинится там же, где и всё остальное: материал называет Алерс, когда сообщает
температуру. `Циркониевые трубки с топливом раскалились выше тысячи двухсот` —
и обе дорожки, через оголение и через рост давления, получают хозяина до того,
как Марго на него сошлётся.

### Куда подать электричество — единственная развилка, которая покупает вывод

Узел `#мониторинг` был снят правильно, а вместе с ним по ошибке ушла развилка,
ради которой он существовал. Без неё «Значит, смена не знала» бралось ниоткуда:
Марго делала вывод о том, что видела смена, ни разу не посмотрев на приборы.

Развилка вернулась в честной форме — как распределение дефицита. Аккумуляторы
снимают с машин на стоянке, их хватает на одно из двух, и Алерс спрашивает,
на что:

| Ход | Что игрок узнаёт |
|---|---|
| `на индикаторы` | все четыре задвижки закрыты — и отсюда вывод про смену |
| `на приводы задвижек` | в аккумуляторе двенадцать вольт, приводу нужно сто двадцать пять; питания хватило только на индикаторы |

Второй вариант не тупик и не наказание: он платит настоящей цифрой
за попытку действовать, а не смотреть, и приводит туда же. Обе дорожки дают
Марго увиденное, а не догадку.

> **Вывод о чужом знании покупается наблюдением. Если персонаж говорит
> «они не знали», игрок должен был перед этим посмотреть.**

### Приборы — это условие, а не возражение

Конденсаторная ветка шла через узел `#мониторинг`: Марго предлагала «сначала
вернуть показания», и **только в ответ** Алерс сообщал, что индикаторы погасли.
Ход брался ниоткуда — до него ни слова не было сказано о приборах, — а ответ
приходил задним числом: игрок узнавал ограничение ровно тогда, когда об него
уже ударился.

Условие переехало туда, где ему место, — в реплику про задвижки: `без управления
они закрываются, а индикаторы гаснут`. Одной строкой предъявлены обе половины
ловушки, и обе опции растут из неё честно: `снять аккумуляторы с машин` —
вернуть себе зрение, `открыть задвижки` — действовать вслепую.

Узел `#мониторинг` после этого исчез: он существовал только затем, чтобы Алерс
сказал то, что должен был сказать раньше. Его работу — дать Марго посмотреть
на приборы — забрала развилка про аккумуляторы, см. выше. **Панчлайн при этом не тронут** —
он и был ради чего вся ветка: «Они были уверены, что работает, и несколько часов
пытались им управлять», и следом её «А он закрылся сразу».

### Вопрос «почему цирконий» стал необязательным

Сюжету он не нужен: причинная
цепь идёт от температуры к водороду и без него. Ценность у него одна — лишняя
развилка и ответ, который стоит того, чтобы его услышать: «Он почти
не поглощает нейтроны. Со сталью топлива понадобилось бы куда больше. Других
достоинств у него нет».

Средняя фраза — не украшение. Без неё «не поглощает нейтроны» остаётся
свойством из справочника, и непонятно, почему за него держались. Со сталью,
которая нейтроны съедает, тот же реактор пришлось бы кормить куда плотнее —
и вот это уже понятная причина выбрать металл, который при тысяче двухстах
отдаёт пару кислород. **Металл выбрали за одно свойство, и убивает он вторым**
— вся игра в двух репликах. Поэтому `#оголилось` теперь
предлагает и `останется водород`, и `почему цирконий`, а формула переехала
в `#оголилось`: пропустивший вопрос ничего не теряет на доске.

> **В ветвистой сцене состояние декорации — такой же референт, как слово.
> Хозяин должен появляться на всех путях, а не на том, по которому писали.**

Тот же класс — цирконий. Через `#оголилось` Алерс даёт температуру и пишет
формулу, и Марго делает вывод. Через `#давление-растёт` она называла цирконий
первой, ниоткуда: игрок этого не знал и не выбирал. Теперь температуру на обеих
дорожках даёт Алерс, а формула оказывается на доске **на всех трёх стратегиях** —
поэтому и `осмотреть доску` после лекции показывает одно и то же.

### Реплики Марго возвращены игроку

Три места выдавали её вторую реплику подряд без хода: `#морская-вода`
(«Блок всё равно потерян»), `#не-знала` («А он закрылся сразу»), `#задвижки`
(«Его выключило отсутствие тока»). Последняя — тезис всей игры, который
по [[10-пролог]] Марго **обязана формулировать сама**, а он приходил бесплатно.

Каждая вынесена в отдельный узел за опцией: `блок всё равно потерян`,
`а он закрылся сразу`, `его выключило отсутствие тока`. Плата — три узла;
получено — правило «Реплики Марго пишет игрок» больше не нарушается нигде.

Третий из них потом убран целиком — не за механику, а за смысл: см. ниже.

### Землетрясение и волна — разные события

Авария начиналась репликой «Реактор заглушен, станция обесточена», и это
слипало две вещи в одну. Заглушила реактор аварийная защита по толчку;
электричество убила вода — волна залила дизели и аккумуляторы часом позже.
Из сцены выходило, будто станцию обесточила сама защита.

Теперь **в корне только землетрясение**: стержни опускаются, цепная реакция
прекращается, топливо продолжает греться. Волна называется уже внутри веток
и ровно там, где объясняет потерю питания, — и называется одинаково на обеих:
`залила дизели и аккумуляторы`. Аккумуляторы здесь не украшение, а посев
под звонок Алерсу в конце пролога.

Побочно это чинит и `#семь`: три числа на доске — расчётная волна, высота
площадки, пришедшая волна — теперь падают на слово, которое в сцене уже звучало.

### Ток на станции и ток в 2038-м — не рифма

Узел `#отсутствие-тока` убран. Марго говорила «Его выключило отсутствие тока»,
Алерс отвечал «Да», и на этом [[08-марго]] строила рифму: через десять лет
Алерса убьёт график отключений.

Рифмы там нет — есть совпадение слова. Обесточенная станция и веерные
отключения 2038-го связаны только тем, что в обоих случаях сказано
«электричество». Тезис игры — про границу, за которой решили не смотреть,
а не про ток; связывать их значило бы продать совпадение за смысл.

**Факт при этом остался в сцене:** задвижки конденсатора закрылись именно
потому, что пропало питание, и Марго упирается в это на обеих подветках.
Убрана только формулировка вслух и строчка про график отключений.

### Люди — это ход, у которого есть цена

`Они не успеют` говорила Марго, и это было и не по-русски точно, и неверно
по фактуре. Настоящая история конкретнее: 12 марта вторую группу послали
к клапану `AO-90` в помещении тора, доза там сразу ушла за предел, и они
повернули назад; один вынес сто шесть миллизиверт. Клапан открыли около
половины третьего переносным компрессором — за час до взрыва.

Теперь это говорит Алерс, и говорит числом. Опция Марго — `отправить людей`,
её реплика — «Отправить людей к клапану. Лимит дозы — не закон физики»,
и она ставит `margo.sent-people`. Читателя у флага пока нет: это то же
«характер ставится впрок», что и `margo.demonstrative`, только цена выше.

Переход из узла делает вторая реплика Алерса — «Давление нашло выход раньше
вас», — и Марго сама называет, где именно: прокладка под крышкой.

### Точность важнее краткости

Приблизительная формулировка хуже длинной. Что переписано:

| Было | Стало |
|---|---|
| «На втором и третьем блоках — да» | «Ставили на блоках поновее — этот семьдесят первого года»: тип, а не перечисление |
| «восемь часов **без единого ватта**» | «восемь часов, электричества не просит» — энергия у него есть, это разница температур |
| «всё, что её пересекает, должно уметь закрыться» | «оболочка держит внутри радиоактивные вещества, трубы бака её пересекают» |
| «задвижки они не двигают» | «двенадцать вольт, приводу нужно сто двадцать пять» |
| «привезти насос **со стороны**» | `доставить насос` |
| «через **пожарную магистраль**» | «трубами пожаротушения» — самоопределяется |
| `цирконий` / `пароциркониевая реакция` | синонимы разведены: `цирконий` / **`радиолиз`** (правда, но литры против кубометров) |

Вентиляция уехала из хаба «чем охлаждать» к такту про взрыв (`почему не
сбросили`): сброс давления не охлаждает, клапан существует ровно чтобы взрыва
не было, и там он причинно на месте.

### Потерянный референт — самый частый класс ошибки

Почти все правки последней вычитки — про одно: фраза верна сама по себе, но
слово в ней указывает на то, чего в сцене ещё нет или что сказано три реплики
назад.

| Что | Куда указывало |
|---|---|
| «Второй случай произошёл позже» | второй случай **чего** — сказано двумя репликами раньше |
| «**Этот** построен в семьдесят первом» | слово «блок» в сцене не звучало ни разу |
| «водород поднимается **под крышку оболочки**» | крышка вводится только следующей репликой |
| «приводу требуется сто двадцать пять» | приводы убрали из реплики Алерса, а ссылка на них осталась |
| «По проекту — восемь часов» | восемь часов **чего** |
| «её дело — не выпустить наружу» | «её» цеплялось и к арматуре, и к оболочке |

Отсюда третий шаг вычитки, к двум предыдущим:

> **Каждое указательное слово — «этот», «его», «там», «оттуда», «ими» —
> проверяется отдельно: есть ли у него хозяин и введён ли он раньше.**

Заодно снят запрет из [[07-оболочка-тз]] на совпадение команд по началу:
автокомплит доразбирает их по следующим буквам, а `из баков` и `из моря` лучше
выдуманной пары ради разных первых букв. Формулировка не должна подгоняться
под ограничение интерфейса.

### Две проверки, которых у меня не было

Мои прогоны мерили длину, структуру и графы — и пропускали ровно то, что видно
глазом за секунду: «держит топливо», «реактор глохнет», «без единого ватта».
Отсюда два обязательных шага вычитки, и оба ручные.

**1. Связность пар реплик.** Выписать каждую пару «последняя реплика узла →
первая реплика следующего», включая переходы через выбор игрока и через
маршруты, и прочитать подряд. Проверять: следует ли вторая из первой, не
появился ли предмет без введения, есть ли у местоимения хозяин.

Что нашлось только этой проверкой:

| Разрыв | Починка |
|---|---|
| На вопрос про **задвижки** Марго отвечала про **приборы**, которых в реплике не было | её ответ переписан на «Никто не видит, работает он или нет» — про бак, а не про приборы |
| Ветка про мониторинг не сообщала, что бак встал, а Марго делала из неё вывод «его выключило отсутствие тока» | ветка и вывод сняты целиком, см. «Приборы — это условие, а не возражение» |
| Зачем искали питание — не сказано | «Задвижки от них не открылись» связывает с предыдущим тактом |
| В сборке «по вашему списку» перечислялись пункты, которых Марго не называла | список приведён к тому, что она действительно назвала |

**2. Русский язык, построчно.** Выписать реплики Алерса и реплики Марго
отдельными списками и вычитать каждую саму по себе.

Что нашлось только этой проверкой:

| Было | Стало |
|---|---|
| «реактор глохнет за две секунды» | «цепная реакция прекращается за две секунды» — глохнет двигатель |
| «аварийная система опускает стержни» | «аварийная защита» — принятый термин |
| «Циркониевые трубки держат топливо» | «Топливо находится в циркониевых трубках» |
| «широкие последствия для людей и среды» | «...для здоровья людей и окружающей среды» |
| «половина ушла в **конденсаторы** турбинного зала» | «половина ушла в турбинный зал» |
| «Восьми часов не было ни одного» | «Из восьми часов, на которые рассчитан конденсатор, он не проработал ни одного» |

Последнее в первой таблице — не стилистика, а ляп: **конденсатором в сцене уже
назван бак**, и «вода ушла в конденсаторы» игрок прочитает как «вода ушла
в изоляционный конденсатор». Слово убрано совсем.

> **Термин, уже занятый в сцене, нельзя переиспользовать в другом значении —
> даже если оба употребления верны.**

### Три правила письма, выведенные на лекции

**1. Вопросы задаёт Алерс.** Марго на них отвечает. Узлы, где она подгоняла его
короткими «Чем?» и «Что?», переписаны: это были не реплики, а кнопка
«продолжить» с репликой вместо подписи. Вопросов у неё осталось четыре, и все
четыре содержательные — «А зачем баку задвижки?», «А почему давление не
сбросили?», «А кто вообще ставит эту отметку?», «И что, все были дураки?».

> **Если персонаж спрашивает только для того, чтобы собеседник продолжил
> говорить, это не реплика.**

**2. Глагол вместо состояния.** Не «реактор заглушен, стержни опущены», а
«аварийная система опускает стержни, реактор глохнет за две секунды». Не
«топливо запаяно в трубки», а «циркониевые трубки держат топливо». Не «оболочка
не сварена целиком», а «оболочку сваривают не целиком». Действие видно, статика
не видна.

**3. Регистр Алерса — инженерный, не разговорный.** Он не скажет «электричества
не просит» — он скажет «электричество для этого не требуется». Не «здесь
другое», а «здесь другая система». Не «есть такая», а «такую систему ставили на
более поздних блоках». Не «кончились к утру», а «баки опустели к утру».

Медиана реплики от этого выросла с семи слов до девяти — и это правильный
размен: точность и характер важнее краткости. Максимум остался четырнадцать.

### Авторское правило нельзя произнести вслух

Ветка «понты вместо ответа» была написана так: Марго отвечает термином
«остаточное тепловыделение», Алерс говорит **«По-русски»**, и она объясняет
обычными словами. Замысел верный — это правило проекта «Сначала по-русски,
потом термин» ([[05-тексты-и-документы]]), сыгранное в лицах.

Реплика невозможна. **Внутри мира все говорят по-немецки**, русский — язык,
на котором написана игра, а не язык действия ([[05-тексты-и-документы]],
«Мир немецкий, текст русский»). Немецкий лектор не может попросить немецкую
студентку сказать что-то по-русски.

Стало: **«Своими словами.»** Тот же смысл, две секунды, никакого языка.

> **Правило письма — про то, как мы пишем, а не про то, что персонажи
> говорят. Формулировку правила нельзя вкладывать им в рот.**

Единственные законные упоминания языка в прологе — те, где речь об акценте
и происхождении: «твоим немецким» на коробке в сцене 4 и «Grünberg? Это же
немецкая фамилия» у Тоби. Оба про человека, а не про язык сцены.

### Шутка про мокрое: выброшена окончательно

Три подхода, и все три хуже сцены без неё.

| Версия | Что не так |
|---|---|
| сосед отвечает полным эхом: «Из чего-нибудь большого рядом» → «Большого и мокрого» | конструкция провисает грамматически |
| шутит сама Марго | не её регистр. Она наглая, но не пошлая, и такую шутку не подаст |
| сосед подхватывает коротко: «…большого поблизости» → «И мокрого» | грамматика чинится, но ветка всё равно тормозит: три такта на то, чтобы прозвучало слово «море», которое и так следующая реплика |

Вывод, который стоит записать:

> **Шутка, которую приходится чинить третий раз, стоит дороже, чем весит.**
> Хорошая реплика не требует, чтобы под неё подгоняли соседние.

Ветка про воду разбирает препятствия по одному, и каждое следующее вырастает
из ответа на предыдущее. `подать воду` — насосы стоят, волна залила дизели и
аккумуляторы. `доставить насос` — сутки по разбитым дорогам, а Марго тут же
поправляет себя: пожарные машины должны стоять прямо на площадке. И вот на это
Алерс предъявляет **два** ограничения одной репликой: линию в реактор открывает
электропривод, а пресная вода в баках кончается.

Дальше развилка бьёт по одному ограничению каждая: `открыть задвижку вручную`
чинит линию и упирается в давление пожарных насосов, `брать морскую воду` чинит
воду и упирается в то, что после неё блок не восстановить. Ни один из вариантов
не «неправильный» — оба делали, и оба опоздали.

Раньше здесь стояло `подать воду извне` сразу после `подать воду` — тавтология,
за которой пропадала вся логика: откуда взялись задвижки, зачем пожарные машины
и почему вообще море. Флаг `margo.demonstrative` по-прежнему без читателя.

### Долг по словам: закрыт

`MARK I` удалён вместе с вклейкой учебника: кольцо добавляло геометрию, путало
причинную цепь и не открывало отдельного решения. `ПАРОЦИРКОНИЕВАЯ РЕАКЦИЯ`
осталась и открывает в корне лекции самостоятельный ход: Марго заранее понимает,
откуда возьмётся водород, и пытается выпустить его.

Это не короткий ответ на тот же вопрос. Без слова игрок занимается охлаждением;
со словом — вентиляцией и упирается в отсутствие питания либо безопасного
доступа. Общая идея слов-шорткатов для других сцен остаётся в
[[99-открытые-вопросы]], но долг этой лекции закрыт.

Вернулись также `вы второй автор`, `кто ставит отметку` и `сколько хватит
денег`. **Не вернулись** насосы с питанием, замедлитель с графитом и шутка про
мокрое: первые два не прошли тест на несущесть, третья висела на снятом
вопросе. Флаг `margo.demonstrative` так и остался без читателя.

### История долга

Вместе с ветками снято всё необязательное — это осознанное промежуточное
состояние, «голая спина», чтобы прочитать скелет чистым. Снятое лежит дословно
в `черновики/01-lecture-вырезанное.md`, с пометкой у каждой группы, почему снята
и на чём висела.

Цена:

| Слово | Что сломано |
|---|---|
| `MARK I` | удалено из актуального набора вместе с веткой про кольцо |
| `ПАРОЦИРКОНИЕВАЯ РЕАКЦИЯ` | открывает стратегию ранней вентиляции в лекции |
| `margo.demonstrative` | ставится в `01-hall#сзади`, читателя нет |

По [[07-оболочка-тз]] неиспользуемое слово было бы ошибкой валидатора. В
актуальном наборе такой ошибки больше нет. `margo.demonstrative` читателя пока
не получил.

Приём, ради которого стоит это помнить: **голую спину можно позволить себе как
рабочее состояние, если цена записана.** Пока цена в тексте, её видно; как
только она не записана, из промежуточного состояния выпускают.

### Чего из ТЗ сделано не было

- **Конкретный провал оценки вместо тезиса «не пересматривают».** Настоящая
  история — расчёт 2008 года, где известное землетрясение перенесли к этому
  берегу и получили пятнадцать метров, а результат назвали предварительным —
  требует трёх реплик и всё равно читается как путаница: посчитали, но решили не
  менять, потому что не было данных, — а как тогда посчитали. Вместо факта
  поставлен механизм: пересчёт опирается на те же записи.
- **`Mark I` в главной линии.** От идеи отказались окончательно: код и кольцо
  убраны из пролога, потому что конкурировали с более простой схемой
  изоляционного конденсатора.

Фактическая база: [руководство МАГАТЭ по INES](https://www-pub.iaea.org/MTCD/Publications/PDF/INES2013web.pdf), [итоговый отчёт МАГАТЭ по аварии](https://www-pub.iaea.org/mtcd/publications/pdf/pub1710-reportbythedg-web.pdf), [схема TEPCO с расчётными и фактическими уровнями цунами](https://www.tepco.co.jp/en/press/corp-com/release/betu11_e/images/110409e9.pdf).

Раскладка повторяет `content/` один в один:

| Здесь | В проекте |
|---|---|
| `пролог/scenes/` | `content/episodes/prolog/scenes/` |
| `пролог/rooms/` | `content/episodes/prolog/rooms/` |
| `пролог/items/` | `content/episodes/prolog/items/` |
| `пролог/words/` | `content/words/` (общие на всю игру) |
| `пролог/docs/` | `content/docs/` (общие на всю игру) |
| `пролог/reference.yaml` | `content/reference.yaml` (справочник, `1`) |

**Имя файла = `id` и менять его нельзя никогда** ([[06-выпуск]]). Всё остальное
правится свободно.

**Id — латиница со скопом по сцене** ([[07-оболочка-тз]], «Имена файлов»):
номер в начале — порядковый номер сцены, и всё, что сцене принадлежит, носит
её номер. Сцена 0 — общага: `00-room`, `00-talk`, `00-book`, `00-toby`. Сцена 1 —
лекция: `01-lecture`, `01-hall`, `01-corridor`, `01-board`, `01-board-after`. Титр берёт номер той
сцены, которую открывает. Слова и документы принадлежат всей игре, поэтому у них
скоп по типу: `word-`, `doc-`.

Предмет, который живёт в двух сценах, номера не меняет — номер говорит, **где он
появился впервые**. Блистер так и остался `00-blister`, хотя лежит ещё и
в `05-room`.

Игрок этого не видит никогда: он вводит `осмотреть учебник`, потому что подпись
берётся из `label`, а не из id. Кириллица осталась ровно там, где её печатает
игрок, — в `label` и в именах узлов-глаголов (`## осмотреть`).

## Замысел не живёт в шапке заметки

> **Во frontmatter только поля. Зачем сцена такая — здесь и в [[10-пролог]].**

Комментарии в шапке заводятся сами собой и тем же вечером расходятся с картой:
в двух местах лежит одно и то же, правят одно. Плюс шапка на тридцать строк
означает, что заметку с текстом сцены нельзя открыть и увидеть текст.

Разрешена одна вещь — **короткая проводка, которую из самого файла не видно**:
«существует только до лекции, перечислен на узле `#до` комнаты». Это факт о
сцеплении файлов, а не рассуждение о замысле.

У `01-lecture` шапки нет вовсе: всё, что там стояло, лежит в разборе выше.
`00-talk` (20 строк) и `02-neukoelln` (23) то же самое ждут.

---

## Сцены

**Точка входа — `scenes/00-intro`:** два кадра без ввода, секунд пять, —
название отдельным экраном, затем лицо Марго и запись под ним. Оттуда игрок
уносит имя героини, город, специальность и то, что право оставаться привязано
к обучению.

Дальше — `rooms/00-room`: шесть целей и командная строка, разговор запускается
по `поговорить с Тоби`.

| id | Сцена в [[10-пролог]] | Дата | | Отдаёт |
|---|---|---|---|---|
| `00-talk` | 1. общага | 12.10.2024, суббота | обяз. | Тоби, абзац из учебника |
| `01-lecture` | 2. лекция | 14.10.2024, понедельник | обяз. | за действия: `АЛЕРС, Ф.`, `ФУКУСИМА`; применение `ПАРОЦИРКОНИЕВОЙ РЕАКЦИИ`; вопрос про стену |
| `02-club` | 3. карнавал и клуб | 08.06.2025, ночь | маршрутность не решена | три монтажных кадра; точка входа в скоп `02` |
| `02-titles` | полицейская запись | 08.06.2025 | внутри сцены 3 | `05:20 ДОСТАВЛЕНЫ ДВОЕ / ОСНОВАНИЕ — ДРАКА` |
| `02-neukoelln` | 3. отделение полиции | 08.06.2025 | внутри сцены 3 | две играбельные части; `margo.procedural` / `margo.confrontational` |
| `03-birthday` | 4. день рождения | 04.07.2025 | **необяз.** | флаг `prolog.box-given` |
| `04-abh` | 5. ведомство | 12.05.2026 | обяз. | Blue Card как предмет, срок в статус-строке; первый источник `TU-2026/117` в ветке `уточнить` |
| `05-dorm` | 6. пустая комната | 05.09.2026 | обяз. | — |
| `06-signature` | 7. деканат | 11.09.2026 | обяз. | бланк; `ОБОСНОВАНИЕ` за `взять`; второй источник `TU-2026/117` |
| `07-abh-2` | 8. ведомство, второй раз | 12.10.2026 | обяз. | подтверждение, что бумага сработала — **не сделано** |
| `07-titles` | титр 2029 | 01.01.2029 | | конец пролога; временная черновая раскладка до добавления сцены 8 |

Нумерация id начинается с нуля, а сцены в [[10-пролог]] считаются с единицы:
`00-` — это первая сцена. Расхождение того не стоит, чтобы переписывать замысел.

Комнаты доигрывают сцены: 2024-й — `00-room`, `01-hall`, `01-corridor`;
2026-й — `05-room`, `06-dean`, `06-corridor`.

**У комнаты один узел на состояние** ([[07-оболочка-тз]], «Комнаты»): она печатает
всё, что видно, четырьмя-пятью строками, а подробности живут в предметах.
Глагола `осмотреться` в игре нет — единственный содержательный ход
`осмотреть <предмет>`. Блок `options` один на заметку: он свойство комнаты.

**Разговор — тоже комната** ([[07-оболочка-тз]], «Диалог»): у `00-talk` узел
`#хаб` без текста, пять тем — опции с `once`, каждая возвращает в хаб. Порядок
выбирает игрок, обязательных тем нет, `Тоби, иди уже` доступно сразу.

`Тоби, иди уже` — реплика за `advance`-командой `▶ иди уже`: в автокомплите она
стоит отдельной последней группой и сообщает, что непройденные темы после неё
исчезнут. `Поговорить с Тоби` не помечается: вход в разговор ничего ещё не
закрывает, точка невозврата — его уход. После разговора таким же маркером
помечено `▶ идти на лекцию`: оно закрывает оставшиеся предметы субботней комнаты.

**Сходиться разрешено только в хаб.** Внутри темы у каждого ответа свой узел:
два варианта, ведущие в одну реплику, — это декорация выбора, а не выбор.
Исключение одно, осознанное: `#кроссовка` перед уходом, потому что уходит
Тоби одинаково.

У `00-room` два узла, и второй — не повтор, а **другое состояние**: `## один`,
та же комната после ухода Тоби.

## Граф

```
00-room ─ поговорить с Тоби ─→ 00-talk ─→ 00-room#один
                                              │
                    01-hall ←─ идти на лекцию
                       │  сесть сзади / в первый ряд
                    01-lecture ─→ 01-hall#после ⇄ 01-corridor
                                          │
                                       02-club
                                          │
                                     02-titles
                                          │
                                  02-neukoelln
                       (маршрутность всего блока не решена)
                                          │
                       03-birthday ───────┤
                        (можно мимо)      │
                   04-abh ─→ 05-dorm ─→ 05-room
                                          │
                                     06-signature
                                          │
                    06-dean ⇄ 06-corridor (телефон)
                                          │
                                  07-abh-2 (todo)
                                          │
                                     07-titles → конец
```

Обе необязательные сцены сделаны выбором на входе: `поехать` / `остаться`,
`пойти` / `не идти`. Отказ ничего не блокирует и никак не отмечается —
он просто уводит на следующую сцену.

## Флаги

Флаг помнит дату сцены, в которой поставлен ([[07-оболочка-тз]], «Флаг помнит,
когда его поставили») — именно поэтому у каждой сцены есть `date:`.

У комнат и предметов своей даты нет: они берут ту, что принесла последняя сцена.
Стартовая — `date:` из `episode.yaml`.

| Флаг                   | Где                                      | Зачем                                                                                        |
| ---------------------- | ---------------------------------------- | -------------------------------------------------------------------------------------------- |
| `prolog.pentest`       | `00-talk#проникновение`                  | назвала это тестом на проникновение — меняет реакцию на подстановку и открывает тёмный ответ |
| `prolog.sharp`         | `00-talk#передам`, `00-talk#справедливо` | вместе с `pentest` открывает «Два. Стоицизм и суицид»                                        |
| `prolog.soft-grunberg` | `00-talk#мимо`                           | промолчала на «ты почти наша»                                                                |
| `prolog.soft-zoloft`   | `00-talk#не-замечает`                    | промолчала на золофт                                                                         |
| `prolog.toby-seen`     | `items/00-toby#осмотреть`                | увидела наклейку Леверкузена — открывает ветку про практику                                  |
| `prolog.leverkusen`    | `00-talk#леверкузен`                     | услышала про журнал приёмки — задел на главу 1                                               |
| `prolog.moscow`        | `00-talk#весь-полёт`                     | рассказала про рейс из Москвы — открывает Беломорканал на выходе                             |
| `prolog.ines`          | вступление `00-talk`                     | нашла устаревшую строчку про седьмой уровень: под завязку `01-lecture`                        |
| `prolog.substitution`  | `00-talk`, ветка «Смотри»                | подурачилась: жопа / дерьмо / Тоби                                                           |
| `prolog.dorm-done`     | `00-talk#выход`                          | Тоби ушёл: гасит `осмотреть Тоби`, открывает `идти на лекцию`                                |
| `prolog.talk-started`  | вступление `00-talk`                     | разговор уже начинался: повторный вход ведёт сразу в `#хаб`, без находки и сплэша            |
| `margo.demonstrative`  | `01-hall#сзади`                          | села в последний ряд. **Читателя нет:** шутка соседа выброшена, характер ставится впрок      |
| `margo.sent-people`    | `01-lecture#ручная-вентиляция`           | предложила послать людей к клапану, зная про дозу. **Читателя нет:** ставится впрок, как и `demonstrative` |
| `prolog.lecture-done`  | `01-lecture#молчание`                    | —                                                                                            |
| `margo.procedural`     | `02-neukoelln#процедура`                 | до угрозы ВНЖ контролирует сведения; после неё требует прочитать запись дословно             |
| `margo.confrontational` | `02-neukoelln#напор`                    | давила точностью. **Читателя нет:** ветвление читает `margo.procedural` и его отрицание, флаг ставится как запись о характере |
| `prolog.neukoelln`     | `02-neukoelln`                           | `Vermerk` 2025 года — единственная бумага, которую в 2038-м выдают сразу и целиком            |
| `prolog.box-given`     | `03-birthday`                            | коробка на полке в эпизоде 2034                                                              |
| `prolog.blue-card`     | `04-abh`                                 | срок в статус-строке                                                                         |
| `prolog.form-seen`     | `06-signature`                           | —                                                                                            |
| `prolog.signed`        | `06-signature`                           | —                                                                                            |
| `prolog.phoned`        | `06-phone`                               | **дата звонка** попадает на бумажку в 2034                                                   |
| `prolog.done`          | `07-titles`                              | пролог пройден                                                                               |

Три ступени расплаты в 2034-м (`пусто` / `коробка` / `коробка + бумажка с датой`)
читаются из `prolog.box-given` и `prolog.phoned`.

## Слова

Серых в прологе нет, потому что игрок и Марго видят одно и то же. Белые не выдаются за само присутствие в сцене: каждое требует действия и каждое что-то открывает.

Целевой набор:

`word-zr` · `word-alers` · `word-fukushima` · `word-tu-2026-117` ·
`word-blue-card` · `word-obosnovanie`

`word-containment` и `word-tu-berlin` удалены: ни то, ни другое не зацепка —
контейнмент переехал в `reference.yaml` (`1`), а TU Berlin это место, где она
находится, а не сведение о ком-то.

Все шесть выдаются действием игрока, ни одной выдачи на входе в сцену или
комнату:

| Слово | Добывается | Применяется |
|---|---|---|
| `ПАРОЦИРКОНИЕВАЯ РЕАКЦИЯ` | `осмотреть доску` до лекции | корневая стратегия ранней вентиляции на лекции |
| `АЛЕРС, Ф.` | «вы второй автор» на лекции; второй путь — `осмотреть бумаги` | 2038 |
| `ФУКУСИМА` | игрок сам называет станцию в конце цепи | 2038 |
| `TU-2026/117` | ветка `уточнить` в мае **и** `осмотреть письмо` в сентябре | 2038 |
| `BLUE CARD` | `осмотреть карту` | сцена 7, ветка про срок и привязку |
| `ОБОСНОВАНИЕ` | `взять бланк` | сцена 8, подача |

## Предметы

| Предмет | id | Где | Глаголы |
|---|---|---|---|
| учебник | `00-book` | `00-room` | осмотреть, листать — обложка и оглавление |
| расписание | `00-schedule` | `00-room` | осмотреть |
| блистер | `00-blister` | `00-room`, `05-room` | осмотреть |
| колонка | `00-speaker` | `00-room` | осмотреть |
| окно | `00-window` | `00-room` | осмотреть |
| Тоби | `00-toby` | `00-room` | осмотреть — пока не ушёл (`if: "!prolog.dorm-done"`) |
| доска | `01-board` | `01-hall#до` | осмотреть — отдаёт `ПАРОЦИРКОНИЕВУЮ РЕАКЦИЮ` |
| доска | `01-board-after` | `01-hall#после` | осмотреть — схема стёрта, три числа остались |
| бумаги | `01-papers` | `01-hall` | осмотреть — пока он их не унёс (`if: "!prolog.lecture-done"`) |
| конспект | `01-notes` | `01-hall` | осмотреть, взять — только после лекции (`if: prolog.lecture-done`) |
| объявление | `01-notice` | `01-corridor` | осмотреть |
| карта | `05-card` | `05-room` | осмотреть, взять |
| бланк | `06-form` | `06-dean` | осмотреть, взять |
| телефон | `06-phone` | `06-corridor` | осмотреть, взять, **позвонить** (только на руках) |

Блистер появляется трижды и каждый раз отдаёт одну строку без содержания.
Ни одного комментария к нему нет и не будет.

**Доска — единственный предмет пролога, который меняется на глазах**, поэтому
она сделана по правилу «предмет, который изменился, — другой предмет»
([[07-оболочка-тз]]): два файла с одним `label`, перечисленные на разных узлах
комнаты. Условий внутри предмета нет, команда у игрока обе сцены одна —
`осмотреть доску`. Формулу в углу Алерс не стирает ни разу: в `#авария` он
освобождает часть доски, в `#семь` стирает только схему, — поэтому
`ПАРОЦИРКОНИЕВУЮ РЕАКЦИЮ` можно взять и после лекции. Пользы от неё там уже нет,
и это осознанно: слово открывает стратегию, а не сокращает объяснение.

У всех узлов `взять` стоит `- once`: иначе комната предлагает взять то, что уже
лежит в кармане, и опция висит в автокомплите до конца эпизода.
Там, где предмет надо не только осмотреть, у комнаты стоит явный блок `options`
с `взять: items` — по умолчанию комната отдаёт только `идти` и `осмотреть`.

`позвонить` живёт в `inHand` телефона, поэтому его нет ни в строке подсказок,
ни в карточке предмета: пока трубка не снята, опции не существует вообще.

## Конфиги

```yaml
# content/episodes/prolog/episode.yaml
id: prolog
title: Берлин, 2024
renderer: academic
entry: scenes/00-intro
itemVerbs: [позвонить]
characters: [margo]
```

`episode.yaml` — единственный владелец настроек эпизода. `game.yaml` хранит
только порядок эпизодов, версию сейва и реестр рендереров; `entry`, заголовок,
рендерер и остальные поля эпизода там не повторяются. Состав и семантика
`verbs` этим решением намеренно не меняются.

## Проверено

Контент прогнан валидатором и двумя проходами на копии проекта:

- `npm run lint` — 0 ошибок; остались шесть предупреждений `dialogue`
  в сценах 3, 4, 5 и 7 — там диалог всё ещё вываливается блоком;
- полный проход доходит до `prolog.done`;
- минималистичный (Тоби выгнан первой же командой, обе необязательные сцены
  пропущены, на выходе снята трубка) — тоже доходит, и `prolog.phoned` стоит.

После переименования проверено заново: ссылки, `exits`, `items`, `give`
и оба прохода живы.

**Сцена 1 переписана после этого прогона и валидатором не гонялась.** Она
пересобрана из картинки: такты переставлены, добавлены такты про конденсатор и
про выброс, турбонасос заменён изоляционным конденсатором, `#тор` переименован
в `#кольцо`, `#сборка` получил `tag: monolog`, ветка с неверным ответом Марго
убрана.

Проверено вручную скриптом: **38 узлов**, 48 опций с меткой и **три**
безусловных маршрута (`#второй-случай`, `#самотёком`, `#конденсатор-после-воды`)
— все три склеивают одинаковые хвосты при разных началах. Битых ссылок нет,
узлов без входа и без выхода нет, единственный внешний выход —
`[[rooms/01-hall#после]]`, ни одна реплика Марго не выдаётся безметочным
маршрутом.

Письмо: ни одной реплики длиннее двадцати слов, ни одного блока тяжелее сорока
пяти, ни одного абзаца тяжелее тридцати, ни одной пары реплик Марго подряд,
ни одной серии длиннее двух реплик Алерса. У Алерса медиана **восемь** слов,
максимум шестнадцать; у Марго медиана **пять**, максимум одиннадцать. Слов
`фланец`, `пожарная магистраль`, `со стороны`, `без единого ватта` в тексте нет.

Все сорок шесть переходов прочитаны попарно — «последняя реплика источника →
первая реплика цели». Разрывов нет ни на одном. У каждой опции в целевом узле
стоит реплика Марго, и её голова совпадает с командой; исключений два, оба
намеренные: `промолчать` на входе и `остаться` в финале — молчание
не произносят, а `остаться` действие, не фраза.

Вопросов у Марго пять, и все содержательные: «А второй случай?», «Почему
цирконий вообще?», «А что вышло наружу?», «А кто вообще ставит эту отметку?»,
«И что, все были дураки?». Прежнее «Должно хватить. В чём проблема?» — чистый
насос — ушло вместе с перестройкой конденсатора.

Ходов игрока: **16 на коротком прохождении** (вода → море → рост давления) и
**23 на длинном** (второй автор, конденсатор целиком с развилкой про питание,
вопрос про цирконий, стравливание водорода по слову, вопрос про отметку).
Против
прежней спины в 19 ходов короткий проход стал короче, длинный — длиннее,
и это правильный размен: разница между прохождениями теперь есть.

**Необязательный ход обязан вернуть игрока туда, откуда увёл.** `кто ставит
отметку` этого не делал: Алерс отвечал про инженера и запас, а следующая
реплика Марго — «Пятнадцать» — отвечала на вопрос про стену, заданный двумя
узлами раньше. На прямой дорожке всё сходилось, на обходной — нет. Теперь Алерс
возвращает свой вопрос дословно: «Так на какую высоту вы бы построили стену?»
Повтор здесь работает и на характер — он не даёт съехать с вопроса.

> **Отложенный вопрос — тот же потерянный референт. Развилка, после которой
> реплика отвечает не на предыдущую строку, ломается ровно так же, как
> «этот» без хозяина.**

**Необязательных ходов в сцене четыре**, и каждый чем-то платит:
`вы второй автор` отдаёт слово `АЛЕРС, Ф.`, `почему цирконий` — реплику про
один нужный признак металла, `кто ставит отметку` — то, что запас считает
живой человек, `это пароциркониевая реакция` — целую подветку за заработанное
слово. Пропустивший все четыре проходит сцену за семнадцать ходов и не теряет
ни одного звена причинной цепи.

**Задвижки не пересекаются ни на одном пути.** Их в сцене два разных вида:
одна на линии подачи воды (`#извне`) и четыре на трубах конденсатора
(`#четыре-задвижки`). Проверено по всем маршрутам: игрок, попавший к линии
подачи, идёт через `#извне` и до конденсатора не доходит; игрок, соскочивший
на конденсатор из водной ветки, `#извне` не проходит и задвижки на линии
не видит. Клапан оболочки (`#стравить`) называется другим словом и рисуется
отдельной линией наружу.

Две опции без реплики Марго — `промолчать` на входе и `остаться` в финале:
молчание не произносят, а `остаться` — действие, не фраза. Та же конвенция
работает в `00-talk`.

---

## Чем это расходится с рыбой в `content/`

Рыбу писали до [[10-пролог]], поэтому расхождения ожидаемые. Всё перечисленное —
осознанные решения, а не забытые файлы.

- **Восемь сцен вместо двух.** В рыбе были `01-lecture`, `04-titles`, `06-signature`.
- **`01-corridor` → `01-corridor`**, добавлен `06-corridor`. Комнат 2024 и 2026
  теперь по паре, и id это отражают.
- **`лаборатория` выброшена.** В [[10-пролог]] и [[03-структура]] её нет; она
  осталась только от старой раскладки «Пролог-2».
- **`items/коробка` выброшена из пролога.** Коробка с переезда — это 2038-й,
  а подарочная коробка живёт внутри сцены 4 и в инвентарь не попадает.
- **Телефон переехал** из деканата в коридор: «на выходе — телефон», и звонить
  из его же кабинета, пока он стоит у окна, нельзя.
- **Карта переехала** в `05-room`: в сцене 6 она лежит на столе.
- **`TU DARMSTADT` в титре → Берлин.** Марго учится в TU Berlin, карнавал —
  в Нойкёльне.
- **`АЛЕРС, В.` → `АЛЕРС, Ф.`** Фридрих.
- Слово «обоснование» получило id `word-obosnovanie`, а не `doc-antrag`: `doc-antrag` уже
  занят документом, а Obsidian и резолвер ищут заметку по имени файла.
- Добавлен документ `doc-tu-2026-117` — пара к `doc-antrag`: смысл в расхождении дат.

## Что здесь решено из «открытого»

- **Blue Card выдана 12.05.2026, действительна до 31.12.2026** (срок договора
  плюс три месяца). Проект TU-2026/117 закрывается 01.10.2026, сообщить об утрате
  позиции надо в две недели. Дата на карте — не та дата, которая её убивает,
  и на карте этого не написано.
- **Немецкий Марго — ремарками**, не поломанными репликами:
  `(по-немецки, быстро, падежи наотмашь)`.
- **Тоби зовёт её Терешковой**, по имени — почти никогда. Она осаживает его
  каждую вторую реплику; молчит только там, где он говорит «у вас же».
- **Алерс по-немецки — Ahlers.** В русском тексте Алерс, в бумагах и подписях
  латиницей — `F. Ahlers`.
- **Ошибка в надписи на коробке — падежная и словарная сразу:**
  `DEM BESTEN SCHACHTELBAUER` (Schachtel — коробка из-под обуви). Алерс
  исправляет одно слово на `BEHÄLTERBAUER`, и получившееся оказывается ровно
  тем, кем он был. Комментария нет.

## Опции и маршруты

`→` с меткой — опция, ждёт ввода. `→` без метки — маршрут, срабатывает сам;
маршруты проверяются сверху вниз, скрытая цель проваливается на следующий
([[07-оболочка-тз]], «Опция и маршрут»).

Из-за этого исчезли три узла-призрака, существовавшие только ради проводки:

| Было | Стало |
|---|---|
| `#без-беломора` с `if: "!prolog.moscow"` | маршрут `→ [[#беломор]]` проваливается на `→ [[#прощание]]` |
| `#поправка` и `#не-говорила` | две опции с `if` прямо в `#протокол` |

**Опция не выбирается автоматически, даже если осталась одна:** опция — это
реплика Марго, и подать её должен игрок.

## Бюджет в словах

Строки меряют экранное место, слова — усилие чтения. Пороги в
[[05-тексты-и-документы]], «Бюджет в словах»: абзац 12 слов (предел 30),
блок 50 (предел 85). На карточки предметов действует только абзацный.

Промер живых сцен против черновых показал, что расходятся **не реплики**
(везде медиана 5–6 слов), а **абзацы прозы**: 5 слов в переписанных против 12
в черновых. Длинный текст здесь — это почти всегда длинная ремарка.

Разбиты без выброшенных слов: открывающая ремарка допроса (41 слово),
первый экран игры (38), карточки `00-toby`, `00-speaker`, `01-board`.

Осталось в черновиках: блоки `04-abh#папка` (113 слов), `04-abh` вступление
(101), `06-signature#уезжаю` (99); абзацы-вступления комнат `05-room` (52),
`06-corridor` (50), `06-dean` (45) и `06-phone#позвонить` (42).

## Бюджет блока

Блок — то, что вываливается игроку между двумя его ходами, считая цепочки
маршрутов. **Блок обязан помещаться на экран** (21 отрисованная строка), цель —
пол-экрана ([[07-оболочка-тз]], «Валидатор»).

Промерены все узлы пролога. Живые сцены чисты, в черновиках осталось:

| Строк | Где | Что там |
|---|---|---|
| 24 | `04-abh#папка` → `карта` → `улица` | **срок Blue Card стоит в середине блока** — при переписывании резать обязательно ([[05-тексты-и-документы]], «Ключевой факт не ставится в середину блока») |
| 19 | `06-signature#уезжаю` → `бланк` | три реплики подряд |

Что уже сделано в живых сценах:

| Правка | Где |
|---|---|
| Подпись стала ходом игрока: `→ подписать`. Блок 22 → 11+11, и игрок сам участвует в почти-побеге, прежде чем Тоби всё ломает | `02-neukoelln#свободны` |
| `tag: monolog` — Алерс собирает Фукусиму на одном дыхании, разбивать нельзя | `01-lecture#сборка` |

**Возврат в хаб маршрутом — не проблема.** Из 73 внутрифайловых маршрутов 21
ведёт в узел без текста; они не добавляют ни строчки. Резать надо по объёму
блока, а не по безусловности перехода: в снятой ветке `#откуда → #ряд-сзади` —
переход условный, а блок всё равно 15 строк.

## Норма объёма

**Строка — до 12 слов, предел 20. Блок — до 40 слов, предел 70**
([[05-тексты-и-документы]], «Норма объёма»). Числа промерены по написанным
сценам, а не назначены.

Живые сцены норму держат. В черновиках осталось:

| Слов | Где |
|---|---|
| 110 | `04-abh#папка` |
| 99 | `04-abh` вступление |
| 98 | `06-signature#уезжаю` |
| 76 | `items/00-book#осмотреть` — карточка предмета, по правилу «описания предметов — исключение по объёму», но проверить стоит |
| 73 | `06-signature#бланк` |

Плюс в `04-abh` и `06-signature` реплики поломаны переносом руками — переносит
оболочка, в заметке реплика пишется одной строкой.

## Как показано, что прошло время

Плейтест отменил прежнюю модель «три разрыва — три инструмента»: дата в углу
статуса склейку не удерживала. Теперь **каждый новый номер сцены открывается
карточкой перехода**, и строит её оболочка ([[07-оболочка-тз]], «Время»).
Ни одной строки прозы про время по-прежнему нет.

Карточка собирается из трёх полей **точки входа в скоп** — того узла, в который
игрок впервые попадает с новым номером. `date:` и `location:` обязательны,
`timeLabel:` ставится, только когда дистанция работает на ритм:

| Скоп | Точка входа | `timeLabel:` | `location:` |
|---|---|---|---|
| `00` | `00-intro` | — | — (стартовый скоп: карточки нет) |
| `01` | `rooms/01-hall` | два дня спустя | `TU BERLIN · АУДИТОРИЯ H 1012` |
| `02` | `02-club` | восемь месяцев спустя | `НОЙКЁЛЬН · КЛУБ` |
| `03` | `03-birthday` | месяц спустя | `TU BERLIN · КАФЕДРА` |
| `04` | `04-abh` | десять месяцев спустя | `ВЕДОМСТВО · ФРИДРИХ-КРАУЗ-УФЕР` |
| `05` | `05-dorm` | четыре месяца спустя | `ОБЩЕЖИТИЕ · ФРАНКЛИНШТРАССЕ` |
| `06` | `06-signature` | шесть дней спустя | `TU BERLIN · КАФЕДРА 2.14` |

Два места повторяются нарочно. `ОБЩЕЖИТИЕ · ФРАНКЛИНШТРАССЕ` стоит на скопе `05`
и рифмуется с субботней комнатой, которой карточки не досталось; `TU BERLIN ·
КАФЕДРА` возвращается через год уже с номером двери. Это единственный способ
сказать «то же место» строкой, которую игрок уже читал.

Скоп `03` датирован пятницей, а открывается объявлением на двери кафедры,
которое к этому дню провисело неделю. Карточка называет кафедру, а не Штеглиц:
квартира на ней означала бы, что игрок туда идёт, — а `пойти` / `не идти` ещё
не выбрано.

**Титр остался, но только там, где есть запись.** `04-titles` был карточкой
из одной даты `12.05.2026` — ровно то, что теперь печатает оболочка, — и удалён;
обе ветки `03-birthday` уходят прямо в `04-abh`. `02-titles` уцелел: кроме
координат в нём есть факт, оформленный как документ, — и первая строка
`08.06.2025` из него снята, её забрала карточка перехода.

После пересборки `02` точка входа — `02-club`, три монтажных кадра перед
отделением. Карточку перехода собирает он: `08.06.2025` · `НОЙКЁЛЬН · КЛУБ` ·
`восемь месяцев спустя`.

**Дата у всего скопа одна, и это восьмое.** Карнавал идёт седьмого, но клуб —
уже ночь, а запись о доставлении помечена `05:20`. Считать по вечеру значило бы
менять `date:` внутри скопа, что запрещено; считать по утру — верно и по факту.

`02-titles` внутри скопа остаётся авторской записью и показывается после сирены,
уже без даты в теле: координаты игрок получил карточкой десять минут назад.

| Было | Стало |
|---|---|
| `08.06.2025` / `05:20 ДОСТАВЛЕНЫ ДВОЕ` / `ОСНОВАНИЕ — ДРАКА` | без даты: две строки записи |
| `12.05.2026` | удалён |
| `2029` / `Ф. АЛЕРС ОСВОБОЖДЁН…` | без изменений — образец |

**Предмет, который не изменился, перестал быть навигацией**, но остался рифмой.
Задуманный учебник, открытый на субботней странице, по-прежнему требует, чтобы
книга ехала с Марго на лекцию: сейчас `00-book` живёт только в комнате и узла
`взять` у него нет. Теперь это украшение, а не долг: ориентацию держит карточка.

## Что осталось открытым

- **Реплика про коробку осталась без места.** «Я двадцать лет учу строить
  коробку. Всё, что нас убивало, было снаружи» снята с финала лекции: в
  аудитории она перегружает конец, где и так стоят «выше в записях не было» и
  «в записях вообще нет ничего, чего ещё не было». Но сказать её он должен —
  на ней [[08-марго]] держит рифму на четырнадцать лет («И через десять лет
  Алерса убьёт график отключений. Снаружи»). Кандидаты на место: телефон
  в `06-corridor`, где он уже говорит плоско и разочаровывающе; деканат
  `06-signature`, где он не объясняет, что делает; день рождения `03-birthday`.
  Не решено, но **потерять нельзя**.
- **Два флага без читателя:** `margo.demonstrative` и `margo.sent-people`. Оба
  ставятся впрок и оба про характер, а не про прогрессию. Второй дороже: игрок
  согласился заплатить людьми. Где это должно вернуться — не решено.
- **У скопа `07` нет `location:`, и поставить его пока некуда.** Сейчас в скоп
  входит `07-titles` — запись 2029 года, у которой места нет и быть не должно.
  Развязывает это сцена 8: точкой входа станет `07-abh-2` (12.10.2026,
  `ВЕДОМСТВО · ФРИДРИХ-КРАУЗ-УФЕР`, `месяц спустя`), а титр окажется внутри
  скопа — и тогда его `date: 01.01.2029` нарушит запрет менять дату внутри
  скопа. Разрешается либо тем, что титр отдаёт свою дату записи («2029» и так
  стоит первой строкой) и берёт дату сцены, либо тем, что он получает
  собственный номер `08`. Выбирать до того, как написана сцена 8, незачем.
- Как называется полицейский в сцене 3 и появляется ли он ещё раз. Пока — нет:
  по [[07-оболочка-тз]] лицо ему не положено, имя, скорее всего, тоже.
- Реплика Алерса в телефоне. Сейчас «Это считали, в девяносто третьем.
  Не прошло по смете» — плоско и разочаровывающе, как задумано, но конкретная
  дата может оказаться лишней.
